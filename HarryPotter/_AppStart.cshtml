@{
    //Global variable telling what the project name is (used in title).
    AppState["projectName"] = "HogwartSim";

    //Global variable telling if this is debug or not, if so, we can throw full blown database errors.
    AppState["debugMode"] = true;
}